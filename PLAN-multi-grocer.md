# Multi-grocer tooling plan

Goal: shopping app supports **Tesco, Sainsbury's, Asda, Morrisons, Waitrose** (UK online shopping/delivery).

## Decision (no fork of basketeer)

- Tesco: **keep basketeer CLI** as the backend (Q1 resolved — no fallback backend).
- Sainsbury's: adopt **open-supermarkets** as a second vendored CLI (has Tesco + Sainsbury's + Ocado).
- Asda / Morrisons / Waitrose: in-house provider modules (no existing tooling; each ~1–2 weeks reverse-engineering + ongoing bot-wall maintenance).
- Q2 resolved: **one retailer per app session** — the user selects a supermarket; item lookup and basket are always scoped to that retailer. Basket items do not carry over when switching retailers.
- Q3 resolved: **never force a delivery-type choice** if avoidable; if one is unavoidable to populate a basket, default to **Delivery**.

## Checklist

- [x] **1. DB migration** — retailer dimension in schema (`grocer_products(retailer, sku, …)`; `sku`/`retailer` on ingredients, persistent_ingredients, shopping_list_items). Done 2026-08-28: `tesco_sku`→`sku` renames, `retailer` columns default `'tesco'`, `tesco_products` cache folded into `grocer_products`, tests updated (97 passing).
- [x] **2. Grocer interface** — Done 2026-08-29: `providers/` package with `Grocer` base class + registry (`get_grocer`/`list_grocers`); basketeer backend moved to `providers/tesco.py` with a `TescoGrocer` adapter; top-level `tesco.py` is now a compat shim; app.py match/refresh/routes go through the registry (routes keep their `/tesco/*` URLs — generalising them is step 7). 97/97 tests pass; adding a retailer = one module + one registry line.
- [x] **3. Sainsbury's** — Done 2026-08-29: open-supermarkets (MIT, v3) added to `package.json`/`node_modules`; backend calls the JS provider library directly (`node -e` with `SainsburysProvider`), not the CLI. `providers/sainsburys.py` implements the full Grocer interface (search, get_product, basket read, basket_set, auth via `~/.sainsburys/session.json` + WC_AUTHENTICATION_ cookie, checkout hand-off URL) and registers as `'sainsburys'`. Sign-in: `scripts/sainsburys_login.js` (headed Chromium, human completes MFA, writes session file) driven by the same background-thread pattern as Tesco. Live-verified anonymous: search/get_product/basket-read all work; basket writes correctly 401 signed-out. 119 tests pass. Build bundles it for free (`build/build.py` copies whole `node_modules` + `scripts/`). App routes for it land in step 7 (UI).
- [x] **4. Asda provider** — complete (recon + live verification 2026-08-29):
  - **API map (verified):** catalogue search/product data = public Algolia index (`8I6WSKCCNV` / `ASDA_PRODUCTS`, key embedded in pages; works from plain `requests` — **live-verified**: search + get_product). Basket = BCC `shopper-baskets` API at `/mobify/proxy/ghs-api/checkout/shopper-baskets/v1/organizations/f_ecom_bjgs_prd?siteId=ASDA_GROCERIES` behind Cloudflare (plain HTTP gets 403; must run inside real Chrome). Guest baskets work — **no Asda account needed**. Item ops: POST `/baskets/{id}/items` body `[{productId, quantity, c_allowSubstitutes}]`, PATCH/DELETE `/baskets/{id}/items/{itemId}`.
  - **Code:** `providers/asda.py` (full Grocer interface, guest-basket auth model, `AsdaGrocer` registered as `'asda'`), `scripts/asda_basket.js` (headless Chrome, persistent profile `~/.asda/recon-profile`, state in `~/.asda/basket.json`, guest-basket-cap cleanup), `tests/test_asda.py` (16 tests). 138/138 tests pass.
  - **Live-verified basket cycle:** set 2× CIN 165468 (£1.75 each) → get ✓ → PATCH to 1 ✓ → set 0 → empty ✓. Items live at `basket.productItems` (shipments[] only carry totals); unit price is `basePrice`, line total is `price`. Earlier Cloudflare IP-throttle cleared on its own (~1h).
- [x] **5. Morrisons provider** — Done 2026-08-29. Full in-house stdlib-urllib backend (no browser, no node): search = `GET /api/webproductpagews/v6/product-pages/search` (SSR-free JSON, `maxPageSize=300&maxProductsToDecorate=200`, products under `productGroups[].decoratedProducts`); basket = BCC-style `GET /api/cart/v1/carts/active` (guest cart tied to `VISITORID` cookie, no sign-in); writes = `POST …/apply-quantity` with additive delta body `[{productId: <uuid>, quantity: <delta>}]` + `x-csrf-token` header (token from homepage HTML `"csrf":{"token":"…"`; missing token → 403 `ecom-csrf-failure`, the 403 first seen with bare curl — it was CSRF, not a WAF). SKU = numeric `retailerProductId`, resolved to the cart's UUID `productId` via one search call. `providers/morrisons.py` registered as `'morrisons'`; recon artifacts + API-map README in `recon/morrisons/`. Guest-cart cookies (`VISITORID`) persist to `~/.morrisons/cookies.json` so the basket survives app restarts. Live-verified end-to-end: search → get_product → set 2 → set 1 → set 0, all confirmed. 171/171 tests pass.
- [x] **6. Waitrose provider** — Done 2026-08-29 (code complete; live verification pending — see note). Stdlib-urllib backend cross-checked against the maintained Go reference client `jingkaihe/waitrose` (cloned to `recon/waitrose/lib/`). Single GraphQL endpoint `/api/graphql-prod/graph/live` + search endpoint `/api/content-prod/v2/cms/publish/productcontent/search/<customerId>?clientType=WEB_APP`; `okhttp/4.12.0` UA + `client-correlation-id`/`breadcrumb` headers. Login = plain email/password (`generateSession`, no MFA) via `~/.waitrose/credentials.json` → session in `~/.waitrose/session.json` (transparent refresh on expiry). **No guest mode** — search and trolley are both keyed on customer/order ids, so sign-in is required (unlike Asda/Morrisons). Trolley updates take absolute quantities (0 removes). Slots/checkout not implemented — website hand-off (Q3: default Delivery if forced). `providers/waitrose.py` registered as `'waitrose'`; 35 new tests, **206/206 pass**. ⚠ **Outstanding:** live search + trolley verification — machine's IP hard-blocked by Akamai at time of writing (TLS completes then dropped/reset; curl and Chrome alike; other sites fine). Expected to clear on its own like Asda's did; retry then per `recon/waitrose/README.md`.
- [x] **7. UI** — Done 2026-08-29: `/grocers` hub page (status, sign-in, "Use for shopping" for all five); all lookup/match/basket routes moved to `/grocers/*` scoped to the active retailer (Q2 — one supermarket for the whole app, basket items are not carried across on switch); old `/tesco/*` routes now redirect to the hub and superseded templates removed; Waitrose credentials-save support added; help copy updated. 208 tests pass; routes smoke-tested live.
- [ ] **8. Packaging** — bundle additional CLI/Node deps into PyInstaller build (see PACKAGING.md).

## Facts

- open-supermarkets registry: UK coverage = Tesco / Sainsbury's / Ocado **only** (no Asda/Morrisons/Waitrose).
- Ocado not in target list but ~free via open-supermarkets (read/basket/slots; no checkout — AWS WAF). Optional add later.
- Pre-migration DB backup: `shopping_app.db.pre-migration.bak` (repo root).
